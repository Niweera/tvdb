RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI} [R,L]

AuthType Basic
AuthName "User"
AuthUserFile "/home/nipuna/.htpasswds/public_html/user/passwd"
require valid-user

